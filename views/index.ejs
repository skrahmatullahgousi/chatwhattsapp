<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <link rel="stylesheet" href="tyle.css"/>
</head>
<body>
    <%for( let data of chats ){%>
        
        <div class="main">
        <div class="from"><P><%=data.from %></P> </div>
        <div class="msg">
            <form method="get" action="/chats/<%=data._id%>/edit">
            <p><%=data.msg %></p></div>
            <button>edit</button>
        </form>
        <form method="post" action="/chats/<%=data._id%>/?_method=delete">
           
            <button>delete</button>
        </form>

        </div>
        <div class="to"><p><%=data.to %>  </p>
           
         <br>
        <div><%=data.date.toString().split(" ")[4]%></div>
        <div><%=data.date.toString().split(" ").splice(0,4).join(",")%></div>
        <form method="get" action="/chats/new">
        <button>newchat</button>
    </form>
        <hr>
    </div>
    <%}%>
</body>
</html>